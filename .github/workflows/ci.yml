name: GitHub Action to Build and Deploy React app to AWS S3
on:
  push:
    branches:
      - main
      - development
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Build
      run: |
        pwd
        yarn
        yarn build